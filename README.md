# ZXM AI Admin 管理平台

个人工具集合管理平台，包含前端管理界面、后端API服务和代理服务。

## 📁 项目结构

```
zxm_ai_admin/
├── admin/          # 前端管理界面 (React + UmiJS + Ant Design)
├── server/         # 后端API服务 (Go + Gin)
├── proxy/          # 代理服务 (Go)
├── Makefile        # 统一构建脚本
└── README.md       # 项目说明文档
```

## 🚀 快速开始

### 前置要求

- **Node.js** >= 20.0.0
- **Go** >= 1.21
- **pnpm** >= 8.0.0

### 安装依赖

```bash
# 安装所有子项目的依赖
make install
```

或分别安装：

```bash
# 前端依赖
cd admin && pnpm install

# 后端依赖
cd server && go mod download

# 代理服务依赖
cd proxy && go mod download
```

### 启动开发环境

**方式一：使用 Makefile（推荐）**

```bash
# 查看所有可用命令
make help

# 分别在不同终端启动各服务
make dev-admin   # 前端 (http://localhost:6806)
make dev-server  # 后端 (http://localhost:6808)
make dev-proxy   # 代理 (http://localhost:6800)
make dev-proxy   # 代理服务
```

**方式二：手动启动**

```bash
# 终端1: 启动前端
cd admin && pnpm dev

# 终端2: 启动后端
cd server && go run cmd/server/main.go

# 终端3: 启动代理服务
cd proxy && go run main.go
```

## 🛠 开发指南

### 前端开发 (admin/)

- 技术栈: React 19 + UmiJS 4.x + Ant Design 5.x
- 开发端口: 6806
- 详细文档: [admin/README.md](./admin/README.md)

### 后端开发 (server/)

- 技术栈: Go 1.21+ + Gin + GORM + SQLite
- 服务端口: 6808
- 详细文档: [server/README.md](./server/README.md)

### 代理服务 (proxy/)

- 技术栈: Go 1.21+
- 功能: 反向代理和请求转发
- 配置: 查看 `proxy/config/config.go`

## 📦 构建部署

### 构建所有项目

```bash
make build
```

构建产物：
- `admin/dist/` - 前端构建产物
- `server/bin/server` - 后端可执行文件
- `proxy/bin/proxy` - 代理服务可执行文件

### 单独构建

```bash
make build-admin   # 仅构建前端
make build-server  # 仅构建后端
make build-proxy   # 仅构建代理服务
```

## 🧹 清理

```bash
make clean
```

## 🧪 测试

```bash
make test
```

## 📝 项目说明

### 为什么采用这种结构？

1. **职责分离**: 前端、后端、代理服务各司其职
2. **技术栈独立**: 每个服务可以使用最适合的技术栈
3. **独立部署**: 各服务可以独立部署和扩展
4. **易于维护**: 清晰的目录结构便于团队协作

### 改进建议

当前项目结构适合中小型项目，如果项目规模扩大，可以考虑：

1. **使用 Monorepo 工具**: 如 pnpm workspaces、nx、turborepo 等
2. **Docker 容器化**: 使用 docker-compose 统一管理开发环境
3. **CI/CD 集成**: 添加 GitHub Actions 或 GitLab CI 自动化构建和部署
4. **统一配置管理**: 使用环境变量或配置中心管理配置

## 📄 许可证

本项目为内部项目，仅供内部使用。

## 🤝 贡献

如有问题或建议，请联系项目维护者。

